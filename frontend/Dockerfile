# GECICI STUB — Osman'in React 18 + Vite uygulamasi bu Dockerfile'i degistirecek.
# Port sozlesmesi ayni kalmali: container icinde 80 (compose 3000:80).
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html
EXPOSE 80
