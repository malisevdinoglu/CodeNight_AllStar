# API Gateway (YARP)

Sistemin tek dis kapisi (`http://localhost:8080`). Sorumluluklar (Faz 2'de tamamlanacak):
JWT dogrulama + header enjeksiyonu, rate limiting, CORS, route tablosu (Core_Principles §9).
